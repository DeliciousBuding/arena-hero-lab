# Contracts — schemas (P9-5 schema ownership migration, 2026-08-12)

> Owner copies of the shared v1 data contracts (user-approved P9 archiving /
> migration, ADR-0003). The workspace root `data/schema/` retains read-only
> mirrors of these files; this directory is the generator-owned source.
> Cross-repo registry: workspace `data/schema/registry.jsonl`.
